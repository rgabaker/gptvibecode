# Dash Enterprise App

## Run locally

```bash
pip install -r requirements.txt
python app.py
```

Open http://localhost:8050

## Dash Enterprise

This repo is ready for Dash Enterprise deployment using:

- `Procfile`: `web: gunicorn app:server`
- `requirements.txt`
